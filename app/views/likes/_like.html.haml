- if Like.find_by(user_id: current_user.id, post_id: @post.id)
  = link_to 'イイね!!をやめる', post_likes_path(post.id), method: :delete, remote: true
- else
  = link_to 'イイね!!をする', post_likes_path(post.id), method: :post, remote: true

